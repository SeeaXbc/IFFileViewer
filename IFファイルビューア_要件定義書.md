# IFファイルビューア 要件定義書（確定版）

> Claude Code での実装を前提とした実装指示書。本書に「未確定」は残さず、既定値で埋めている項目は「※既定値」と明記する。変更したい場合はここを書き換えてから実装させる。

---

## 1. プロジェクト概要

- **目的**：システム間連携で使用するIFファイル（拡張子なしを含む）、CSV/TSV、任意区切りテキスト、およびバイナリファイルを閲覧する。
- **位置づけ**：**読み取り専用ビューア**。元ファイルへの書き込みは一切行わない（構造的にファイル破壊リスクをゼロにする）。
  - 補足：オプトインの**編集モード**（9章）を持つが、編集結果は**常に別ファイルとして新規保存**され、元ファイルには一切書き込まない（この原則は編集モードでも不変）。
- **対象プラットフォーム**：Windows（会社PC想定）。モダンブラウザ（Chrome/Edge）で動作。
- **実装形態**：**単一HTMLファイル**。ブラウザにドラッグ&ドロップで表示。インストール不要・管理者権限不要。
- **大容量対応**：不要（仮想スクロール等は初期スコープ外。ただし数万行程度で固まらない実装は意識する）。これはテキストモードの想定であり、バイナリモードは行数が膨らみやすいため別途表示上限を設ける（5.5参照）。

---

## 2. 技術方針

- **構成**：単一HTMLファイルにHTML/CSS/JavaScriptを内包。外部CDN依存は避け、必要なコードは埋め込む。
⇨オフラインで使用可能であれば単一でこだわらなくてOK。配布が必要な場合はZIPにかためて送るなどを想定。
- **ファイル読み込み**：File API。ドラッグ&ドロップおよびファイル選択。`FileReader` / `ArrayBuffer` で生バイトを保持。
- **文字コードのデコード**：ブラウザ標準の `TextDecoder`（`shift_jis` / `utf-8` / `utf-16le` / `utf-16be` / `euc-jp` / `windows-1252` 等）。
  - 注記：WHATWG Encoding準拠のため、`shift_jis` ラベルは実質CP932相当のデコードになる（本ツールにとっては好都合）。
  - 注記：`ascii` というラベルは存在しない（指定すると windows-1252 に解決される）。ASCII表示は自前実装とする（0x20〜0x7E を文字表示、それ以外は `.`）。
- **書き込み**：元ファイルへは行わない。編集モード（9章）の保存は File System Access API による**別ファイルの新規作成**のみ。
- **状態保持**：仮想ヘッダー定義やマーカー設定は `localStorage` に保持してよい（ファイル本体には書かない）。
  - 注記：`file://` で開く場合、企業ポリシーやブラウザ設定により localStorage が使えないことがある。**localStorage が使えない環境でも閲覧機能はすべて動作すること**（諦めるのは設定の永続化のみ。仮想ヘッダー定義はJSONファイル読み込みで代替できる。4.3参照）。

---

## 3. 表示モード

1ファイルを開いた状態で、ツールバーから **テキスト整列モード ⇔ バイナリモード** を随時トグルできる。※既定値（種類ごとの固定ではなく、常に手動切替可能とする）

- **開いた直後の初期モード**：簡易判定で自動選択する。先頭ブロック（数KB）にNUL等の非テキストバイトが一定割合以上含まれればバイナリモード、それ以外はテキスト整列モードで開く。※既定値。以後はユーザーの手動トグルが優先。

---

## 4. テキスト整列モード

### 4.1 区切り文字と行の分割
- カンマ、タブ、任意の指定文字を区切り文字として選択できる。
- **複数の区切り文字を同時に有効化**できる（例：カンマ＋半角スペースの2つを区切りとする）。同じ位置に複数の区切りが一致し得る場合は**長い区切りを優先**する（longest match）。※既定値。
- **任意の区切り文字は複数登録**でき、以後は選択肢として保持する（localStorage保存。不可環境ではセッション内のみ）。登録解除はチップの**右クリック**または設定画面から。
- 区切り文字はUIから随時変更でき、再パースして表示更新する。
- **クォート処理は行わない（単純split）**：`"値に,カンマ"` のような引用符付きフィールドも区切り文字で機械的に分割する。生データがそのまま見えることを優先するビューアの思想による。※将来、クォート対応(RFC 4180)をオプションとして追加する余地は残す（初期実装では作らない。7章参照）。
- **改行コード**：既定は**自動判定**（CRLF / LF / CR のいずれも行区切りとして扱う）。加えて**CRLF / LF / CR を明示指定**でき、指定時はその改行のみで行分割し、他の改行文字はデータの一部として扱う。※既定値：自動。混在ファイルへの特別対応は不要（自動判定が全種を行区切りとするため実用上問題ない）。
- 検出した改行コード種別と末尾改行の有無をステータスバーに表示する（明示指定中は指定値と検出値を併記）。※既定値。

### 4.2 整列表示（2方式を切替）
- **グリッド表示**：セルをグリッドに分割。区切り文字は境界として非表示。
- **桁揃え表示**：区切り文字を表示したまま、各列の幅だけを揃える。
  - パディングは**CSS上の表示幅調整のみ**。スペース等を実データやDOMのテキストに挿入しない。
  - したがって、桁揃え表示から範囲選択してコピーした場合も**パディング文字が混入せず、元データのテキストが得られる**こと（受け入れ基準に含む）。
  - **区切り文字の後ろに表示上の間隔**（0〜16文字の任意指定）を設定できる。桁揃え表示では区切り文字の直後、グリッド表示では列の右余白として、**両方式に適用**する。これもCSSのみで実データ・コピーに影響しない。
  - 等幅フォントを使用（既定: BIZ UDゴシック。6章のフォント設定参照）。
  - タブ文字は**1文字幅で描画**（`tab-size:1`）し、列幅計算（タブ=1文字）と一致させる。タブ区切りを見やすくする場合は区切り後間隔または不可視文字の可視化を使う。

### 4.2.1 行番号表示
- 各行の左に行番号を表示する（1始まり、表示ON/OFF切替可）。※既定値。行番号は表示層のみでデータには含めない（コピー対象外）。

### 4.2.2 不可視文字の可視化
- スペース（`·`）、タブ（`→`）、全角スペース（`□`）、行内に残ったCR（`␍`）を淡色で可視化するトグルを持つ。※既定値：OFF。
- 可視化は**CSS疑似要素のオーバーレイのみ**で行い、DOM上のテキストは元の文字のまま保持する。したがって可視化ON中にコピーしても元データが得られる。

### 4.3 仮想ヘッダー機能
- 実データに干渉せず、表示層に説明用ヘッダー行を被せる（sticky表示でスクロール時も固定）。
- **複数行**のヘッダーを設定可能。
- 列定義はJSONで外部保持し、**ファイル名パターンで自動適用**する。※既定値：定義はJSON手書き前提（GUI編集は将来対応）。
- **定義の登録・管理方法（両対応）**：
  1. 設定画面のテキストエリアにJSONを貼り付けて登録 → `localStorage` に保存（次回以降も有効）。
  2. 定義JSONファイルをドラッグ&ドロップで読み込み（そのセッション中有効）。localStorage が使えない環境ではこちらで運用する。
- **複数の定義が `match` に一致した場合**：定義リストの先頭一致を自動適用し、UIから別の定義に切り替えられる。※既定値。
- **定義の削除**：設定画面の「登録済みヘッダー定義」一覧から個別削除（✕）／一括削除できる。削除時は開いているタブの手動選択インデックスを補正し、削除された定義を使用中のタブは「自動」に戻す。**編集モード中のタブが使用している定義は削除・変更不可**（列構造が変わりバイト完全性の前提が崩れるため。編集モード終了後に操作する）。定義JSONのドロップ追加時も手動選択中のタブの選択を維持する。
- **定義の `encoding` / `delimiter` は初期値**として適用する。UIからの手動変更が常に優先（定義適用後も随時変更可）。※既定値。
- ただし**BOM付きファイルはBOM判定を定義の `encoding` より優先**する（BOMは確定的な情報のため）。※既定値。
- **列詳細のツールチップ表示**：定義に `details`（列ごとの詳細説明の配列。**任意項目**）を持たせられる。
  - `details` がある列は、仮想ヘッダーのセルに**点線下線＋ヘルプカーソル**の目印を付け、**マウスオーバーで詳細をツールチップ表示**する（遅延なしの自前ツールチップ。1列目のヘッダー文言と列番号をタイトルとして併記）。
  - 説明文中の `\n` で複数行表示できる。
  - 表示層のみの機能であり、実データ・DOMテキスト・コピー結果には一切影響しない。
  - `details` が無い定義・列は従来どおり動作する（後方互換）。不正な `details`（配列以外等）は黙って無視する。
- JSONスキーマ（例）：
  ```json
  {
    "name": "受注IF",
    "match": "IF_ORDER_*",
    "delimiter": ",",
    "encoding": "shift_jis",
    "headers": [
      ["顧客ID", "受注日", "金額", "区分"],
      ["必須",   "YYYYMMDD", "整数", "1=通常/2=返品"]
    ],
    "details": [
      "顧客マスタのID。数字8桁固定・先頭0埋め。\n存在しないIDは取込エラーとなる。",
      "受注日。YYYYMMDD形式（例: 20260401）。\n未来日付は不可。",
      "税込金額。整数のみ（カンマ・小数点不可）。",
      "受注区分。1=通常 / 2=返品。"
    ]
  }
  ```
  - `match`：ファイル名のワイルドカードパターン。読み込んだファイル名に一致した定義を自動適用する。
  - `details`：列ごとの詳細説明（任意）。ヘッダーセルへのマウスオーバーでツールチップ表示する。

### 4.4 マーカー機能（テキスト）
- 指定文字列・行番号・列番号をハイライト。
  - 文字列マーカーは**大文字小文字を区別する**（マーカーごとのトグルで「区別しない」に切替可）。※既定値。
  - 文字列マーカーは登録時の**「.*」トグルで正規表現マーカー**にできる。検索と同じ仕様（JavaScript構文・行単位・長さ0の一致は無視）で、不正なパターンは件数欄に「無効」と表示する。コンパイル済み正規表現はキャッシュして行ごとの再コンパイルを避ける。
  - 列番号マーカーは**該当列全体を背景色でハイライト**する。列番号は1始まり。※既定値。
- 複数登録可能。各マーカーに以下を持つ：色、有効/無効トグル、登録解除、出現回数のリアルタイム表示。
- サイドパネルで一覧管理。

### 4.5 補助操作（ジャンプ・列コピー・行フィルタ）
- **行番号ジャンプ**：`Ctrl+G` で行番号を指定してジャンプし、対象行を一時ハイライトする。バイナリモードではオフセット（10進/0x16進）ジャンプになる。
- **列コピー**：ヘッダーセルのクリック、またはデータセルの `Alt`+クリックで、その列の全行の値（1行1値）をクリップボードへコピーする。
- **行フィルタ**：検索グループの「⊜ ヒット行」トグルで、検索ヒットを含む行のみを表示する（grepイメージ）。行番号は元ファイルのまま表示。フィルタ外の行への行番号ジャンプはフィルタを自動解除する。

### 4.6 固定長ファイル対応
- ヘッダー定義（4.3）に `widths`（各列の**バイト幅**の配列。例 `[6,20,5,9]`）を書くと、区切り文字ではなく**バイト幅で列分割**する。COBOL系の固定長レイアウトを想定。
- バイト幅は選択中のエンコーディングで計算する（Shift-JIS/EUC-JP: 全角2バイト・半角カナ1バイト、UTF-8: 日本語3バイト、UTF-16: 2バイト/単位）。編集モードと同じエンコーダ（9.2）の文字バイト長を共用する。
- 定義の合計幅より長い行は超過分を「余り列」として右端に表示する（レイアウト不正の可視化）。フィールド境界をまたぐ文字は次列へ送る。
- 固定長適用中は区切りチップの代わりに「固定長: n列 / 計mバイト」を表示し、区切り追加を無効化する。
- **編集モードとの連携**：セル確定時にフィールドのバイト幅を自動維持する（エンコード後のバイト長が幅未満なら右側を半角スペースでパディング、幅超過は理由付きで確定拒否）。

### 4.7 列プロファイルと検査
- **列プロファイル（📊 列情報）**：全行を対象に列ごとの「値あり件数・空欄件数（列自体がない行は別掲）・ユニーク数（5,000超で打ち切り）・文字数の範囲・型推定（数値/日付/ほぼ数値…）」をモーダルで一覧表示する。
- **検査（✔ 検査）**：ヘッダー定義に `rules` 配列を書いたファイルで検査ボタンが現れる。ルールは `{"col": 列番号(1始まり), "type": "number"|"date", "required": true, "pattern": "正規表現"}`。
  - NGセルは**赤枠＋理由のツールチップ**で表示し、ツールバーにNG件数、「NG行へ▼」で順にジャンプ。
  - `date` は YYYYMMDD / YYYY-MM-DD / YYYY/MM/DD を実在日付として検証（月末日チェックあり）。
  - 編集モードでセルを修正すると該当行のみ即時再検査される。

---

## 5. バイナリモード（Stirling風）

### 5.1 レイアウト
```
オフセット   HEXダンプ（16バイト/行、8バイトで区切り）           デコード結果（16セル）
00000000    48 65 6C 6C 6F 2C 20 57  6F 72 6C 64 21 0D 0A E3   H  e  l  l  o  ,     W   o  r  l  d  !  .  .  あ
00000010    81 82 ...                                          ・  ・ ...
```
- **左**：オフセット（16進アドレス）列。
- **中央**：1行あたり16バイトのHEX表示。8バイト目と9バイト目の間に区切りを入れる。
- **右**：指定文字コードでのデコード結果（後述の位置揃え方式）。

### 5.2 文字コード指定
- デコードに用いる文字コードをドロップダウンで選択：Shift-JIS(CP932) / UTF-8 / UTF-16LE / UTF-16BE / ASCII / EUC-JP / Latin-1 など。
- 切替時に右ペインを再描画する。

### 5.3 デコード結果の位置揃え方式【確定】
**各文字をHEX側のバイト位置に揃えて描画する。**

- 右ペインはHEXと同じ16セルのグリッドとし、各セルが対応する1バイトに紐づく。
- マルチバイト文字（UTF-8/16、Shift-JISの全角等）は、**その文字の先頭バイトのセルに文字を配置**する。
- 続く後続バイトのセルは**継続表示**とする。※既定値：継続セルは淡色の `·`（中黒）を表示。設定で「空白」に切替可としてよい。
- デコードできないバイト（不正シーケンス、制御文字）は `.` を表示する。
- 文字が16バイトの行境界をまたぐ場合は、**バッファ全体を通しでデコードしてからバイトオフセットでセルにマッピング**する。先頭バイトが属する行のセルに文字を置き、続きは次行の継続セルにする。
- UTF-16選択時のBOM（`FF FE` / `FE FF`）はスキップせず、**通常の2バイトとしてセルに表示**する（バイナリの実体を隠さない）。※既定値。
- **実装注記（本ツール最大の難所）**：`TextDecoder` は「どのバイトがどの文字になったか」の対応を返さない。バイト位置マッピングには、`TextDecoder` に `{stream: true}` で1バイトずつ流し込み、文字が確定した位置を検出する**増分デコードの自前実装**が必要。工数見積もり時に考慮すること。

### 5.4 マーク機能（バイナリ）【確定：バイト列パターン指定】
- ユーザーが**バイト列パターン**を16進で入力してマークする（例：`0D 0A`、`FF FE`、`89 50 4E 47`）。
- 一致した全出現箇所を、**HEXダンプ側とデコード側の両方に連動**してハイライトする。
- 複数パターンを登録可能。各パターンに：色、有効/無効、登録解除、出現回数のリアルタイム表示を持つ。
- 入力は空白区切りの16進（大文字小文字・空白有無を許容し、内部で正規化）。
- **制御コードのプリセット**：一般的な制御コード（NUL/SOH/STX/ETX/EOT/ENQ/ACK/TAB/LF/CR/CRLF/DLE/NAK/SYN/ESC/FS/GS/RS/US）をマーク候補としてあらかじめ用意する。**初期状態はすべてOFF**で、出現回数のみ表示。サイドパネルでは折りたたみ表示とし、ユーザー登録分と区別する。

### 5.5 表示量の上限
- HEXダンプは行数が膨らみやすい（1MB = 65,536行）ため、**初期表示は先頭256KB（16,384行）**とし、「さらに表示」ボタンで256KBずつ追記描画する。※既定値。
- マーク（5.4）・検索（6章）の**出現回数カウントは表示範囲ではなくバッファ全体**を対象とする。未表示領域のヒットへ移動する場合は、該当位置まで表示を自動拡張してジャンプする。

### 5.6 対応バイトの選択ハイライト
- HEX側のバイト、またはデコード側の文字を**クリックすると、対応するバイト/文字を両ペインで相互にハイライト**する。
- マルチバイト文字の場合は、その文字を構成する**全バイト**を選択範囲とする（どのバイトをクリックしても文字全体に拡張）。
- ステータスバーに選択位置（オフセット16進/10進）、バイト値、デコード文字を表示する。
- 再クリックまたは Esc で解除。※既定値。

---

## 6. 共通機能

- **複数ファイルの表示**：**タブ切替**で複数ファイルを保持・閲覧できる。ペイン分割（並べて比較）は初期スコープ外（7章参照）。
- **表示拡大率**：表示部分の拡大率を変更できる（50〜300%）。※既定値：100%。
- **ヘルプ**：ツールバーの「❓ ヘルプ」から、基本操作（ファイルの開き方、区切り・改行・表示方式、仮想ヘッダー定義、マーカー、バイナリモード、検索、ショートカット、設定の保存）を記載したヘルプページ（モーダル）を表示する。
- **フォント選択**：既定は **BIZ UDゴシック**（Windows 10/11標準搭載。0とOの判別がしやすく日本語対応）。MS ゴシック / MS 明朝 / カスタム（CSS font-family直接指定）に切替可能。桁揃え表示・バイナリ表示は「半角:全角＝1:2」の等幅フォントを前提とし、それ以外では桁ずれが起こり得る旨をUIに注記する。
- **文字コード指定（読み込み時）**：Shift-JIS(CP932) / UTF-8 / UTF-16 等を明示指定。補助的に自動判定を用意（BOM判定＋簡易推定）。
- **検索**：テキストモードは文字列検索、バイナリモードは16進パターン検索。いずれもヒット箇所をハイライトし、前後移動できる。
  - テキスト検索は大文字小文字を区別する（トグルで「区別しない」に切替可）。※既定値。
  - テキスト検索は**通常検索と正規表現検索を「.*」ボタンでトグル切替**できる（VSCode風）。正規表現はJavaScript構文・**行単位**で照合し、長さ0の一致は無視、不正なパターンは件数欄に「無効」と表示する。ヘルプページに正規表現サンプルを記載する。※既定値：通常検索。
  - 検索ヒットのハイライトはマーカー色より**前面**に表示する（検索は一時的な操作のため）。※既定値。

---

## 7. 非スコープ（初期実装で作らないもの）

- フルテキストエディタ（任意位置の挿入・削除・複数行編集）。※セル単位の編集は9章で対応済み。行の追加・削除も非スコープ。
- 2ファイル比較（diff）。※検討の結果、不要と判断（2026-07）。
- GB級の大容量ファイルの最適化（仮想スクロール等）。
- 固定長（カラム位置指定）IFの分割表示。※将来対応。列定義に開始位置・幅を持たせる拡張を想定するが初期実装では作らない。
- 仮想ヘッダー定義のGUIエディタ。※将来対応（初期はJSON手書き）。
- ペイン分割表示（複数ファイルを並べて比較）。※将来対応（初期はタブのみ）。
- CSVのクォート処理（RFC 4180）。※将来対応（初期は単純split。4.1参照）。

---

## 8. 受け入れ基準（抜粋）

1. 拡張子なしファイルをドラッグ&ドロップで開ける。
2. カンマ/タブ/任意区切りで、グリッド表示と桁揃え表示を切替できる。桁揃え表示では区切り文字が見えたまま列が揃う。
3. 仮想ヘッダーJSONをファイル名パターンで自動適用でき、実ファイルは変更されない。
4. バイナリモードで、Shift-JIS/UTF-8を切り替えると右ペインのデコードが更新され、全角文字が先頭バイト位置に揃って表示される。
5. `0D 0A` を登録すると、HEX側・デコード側の両方で該当箇所がハイライトされ、出現回数が表示される。
6. いかなる操作でも元ファイルが書き換わらない。
7. 桁揃え表示から範囲選択してコピーしても、パディング文字（区切り後間隔を含む）が混入せず元データのテキストが得られる。
8. 性能の目安：5万行×20列程度のCSVで、区切り文字変更・表示方式切替が体感1〜3秒以内に完了する。10MBのバイナリファイルを開いてもUIがフリーズしない（先頭256KBのみ初期表示）。
9. localStorage が無効な環境でも、ファイル閲覧・モード切替・マーカー（セッション内）・定義JSONファイルの読み込みが動作する。
10. 編集モードでセルを編集して保存したファイルは、**編集セル該当箇所以外の全バイトが元ファイルと完全一致**する（Shift-JIS / EUC-JP / UTF-8 / UTF-16、BOM付き含む）。1セルも編集せずに保存した場合は全バイトが一致する。
11. 保存先フォルダを一度選択すれば、以後そのセッション中はダイアログなしで保存できる。同名ファイルは上書きせず連番付きで新規作成される。
12. 区切り文字・改行・選択中の文字コードで表現できない文字を含む値は確定できない。デコードが非可逆な行は編集不可となるが、他の行の編集と保存は可能。
13. 編集モードを含むいかなる操作でも元ファイルは書き換わらない。
14. `widths` 付き定義を適用すると、Shift-JISの全角/半角混在行がバイト幅どおりに列分割される。固定長ファイルのセル編集後も各フィールドのバイト幅が維持され、未編集部分のバイトは元ファイルと一致する。
15. `rules` 付き定義で「✔ 検査」を実行すると、必須空欄・数値/日付不正・パターン不一致のセルが赤枠＋理由付きで表示され、件数表示とNG行ジャンプが機能する。
16. 「⊜ ヒット行」で検索ヒット行のみに絞り込め、行番号は元ファイルの行番号のまま表示される。
17. 編集モード中の `Ctrl+Z`/`Ctrl+Y` で編集操作単位のUndo/Redoができる。

---

## 9. 編集モード（セル単位編集）

### 9.1 位置づけと前提
- 既定は従来どおり読み取り専用。ツールバーの「✏ 編集」トグルで**明示的にオプトイン**する（テキストモードのみ）。
- **編集中は区切り文字・改行指定・文字コード・ヘッダー定義・表示モードを変更できない**（UIをロック）。列構造とバイト完全性の前提を守るため。
- 行の追加・削除、区切り文字自体の編集は対象外（セルの値のみ）。

### 9.2 バイト完全性の保証（本章の核）
- 編集モード開始時に元バイト列を**バイトレベルで行分割**して保持する（改行バイト 0x0D/0x0A は対応エンコーディングのマルチバイト文字中に現れないため安全。UTF-16は2バイト単位で判定）。開始時に「行分割の再構成＝デコード結果」の完全一致を検証し、不一致ならモード開始を拒否する。
- 保存ファイルは `BOM + Σ(編集行 ? 再エンコード : 元バイト無加工コピー + 改行の元バイト無加工コピー)` で構築する。**未編集行・改行コード・BOMは1バイトも変わらない**。
- 行の初回編集時に「デコード→再エンコード＝元バイト」の可逆性を検証し、不一致の行（不正バイト、CP932の重複マッピング領域等）は**その行のみ編集不可**とする。
- Shift-JIS / EUC-JP への再エンコードは、`TextDecoder` に全バイト組合せを流して**実行時に生成した逆引き表**で行う（変換テーブルの埋め込み不要・単一HTML維持）。UTF-16LE/BE は自前変換、UTF-8 は `TextEncoder`。

### 9.3 編集操作
- セルを**ダブルクリック**でインライン編集。Enter=確定 / Esc=取消 / Tab・Shift+Tab=確定して隣のセルへ移動。
- 確定時バリデーション（いずれも確定拒否＋理由表示）：①有効な区切り文字を含む ②改行を含む ③現在の文字コードで表現できない文字を含む。
- 編集済みセルは橙色の枠で明示。**右クリックで個別に元に戻す**、「全て元に戻す」で一括取消。ステータスバーとツールバーに編集セル数を表示。
- 未保存の編集がある状態での編集モード終了・タブクローズ・ページ離脱時は確認を出す。編集モード終了時は表示を元ファイルの内容に戻す。

### 9.4 保存（File System Access API）
- 「📁 保存先...」で保存先フォルダを選択（`showDirectoryPicker`）。ハンドルは IndexedDB に保存し、次回以降復元する（Chrome/Edge 122+ では「今後も許可」で再起動後も無確認。それ以外は権限確認1クリック）。
- **保存名入力欄**（初期値=元ファイル名）の名前で、保存ボタン押下時に**ダイアログなしで新規ファイル作成**。同名が存在する場合は `名前 (1)` `名前 (2)`…と連番を付け、実際の保存名をトースト通知する。
- 元ファイルの上書きは構造的に不可能（ブラウザは開いた元ファイルへの書き込み手段を持たない）。
- File System Access API 非対応ブラウザ（Firefox等）では保存ボタンを無効化し、Chrome/Edge の使用を促す（対象プラットフォームは Chrome/Edge）。

---

## 10. 開発・保守

配布物は従来どおり `IFファイルビューア.html` 単一ファイル。以下はリポジトリ上の開発用資産で、配布には含めない。

### 10.1 テスト（`tests/`）
- Node.js 標準テストランナー（`npm test`）＋ Playwright（Chromium）による E2E テスト。
  - `edit-integrity.test.js`：**バイト完全性の回帰テスト**（全対応エンコーディングで「未編集保存＝全バイト一致」、編集行のエンコード正当性、確定バリデーション）。編集モードの核心保証を機械的に担保する。
  - `fixed-length.test.js`：固定長分割・幅維持編集・保存整合。
  - `features.test.js`：検査・列プロファイル・行フィルタ・Undo/Redo・ジャンプ・列コピー。
- ローカル実行：`npm install` 後 `npm test`（Chromium は `PLAYWRIGHT_BROWSERS_PATH` / `~/.cache/ms-playwright` / `CHROMIUM_PATH` から自動検出）。

### 10.2 型検査（`npm run typecheck`）
- `tools/typecheck.mjs` が単一HTMLから `<script>` を抽出し、`tsc --checkJs` で検査する（現在エラー0件）。
- 件数ベースライン方式（`tools/tsc-baseline.json`）：新規エラーの混入でCIが失敗する。意図的な場合は `--update` で更新。
- 主要データ構造（HeaderDef / EditState / Tab）は HTML 冒頭の JSDoc typedef を参照。

### 10.3 CI（`.github/workflows/test.yml`）
- push / pull_request ごとに型検査とE2Eテストを GitHub Actions（ubuntu / Node 22 / Chromium）で実行する。

### 10.4 バージョン運用
- HTML内の `APP_VERSION` 定数がヘルプ画面に表示される。機能追加時は `APP_VERSION`・`package.json` の version・本書の更新履歴を同時に更新する。

---

## 11. 更新履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0.0 | 2026-07-05 | 初版（読み取り専用ビューア：テキスト整列・バイナリ・マーカー・検索・仮想ヘッダー定義） |
| 1.1.0 | 2026-07-07 | 編集モード（9章）：セル単位編集・バイト完全性保証・所定フォルダへの保存 |
| 1.2.0 | 2026-07-07 | 固定長ファイル対応（4.6）・列プロファイルと検査（4.7）・行フィルタ/ジャンプ/列コピー/Undo・Redo（4.5）・開発保守基盤（10章：テスト/型検査/CI） |
| 1.3.0 | 2026-07-08 | ヘッダー定義の削除UI（設定画面の一覧から個別/一括削除）。定義追加・削除時のタブ選択インデックス補正、編集モード中の定義変更ガード |
